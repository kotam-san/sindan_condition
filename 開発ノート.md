# 後継者のコンディション診断 — 開発ノート

最終更新：2026年3月21日（第4回）

---

## 現在の実装状況

### 完成しているもの

`sindan_condition/index.html` — 単一ファイルで動作するReact診断ツール

| 機能 | 状態 |
|------|------|
| 16問 1問1画面（軸シャッフル順通り） | ✅ 動作確認済み |
| 5段階選択 → 自動で次の問へ（350ms） | ✅ |
| 戻るボタン（回答を取り消す） | ✅ |
| プログレスバー | ✅ |
| 自由記述 + スキップボタン | ✅ |
| ステージ判定（1/2/3） + スコア表示 | ✅ |
| レーダーチャート（SVG）ユーザー実線 + 平均破線 | ✅ |
| 軸スコアバー（最高軸を▲最高で強調） | ✅ |
| 特徴コメント（最高スコア軸ベース） | ✅ |
| 比較コメント（平均との差） | ✅ |
| **Gated Content設計（ゲート前/ゲート/ゲート後）** | ✅ |
| **メアド入力ゲート（リデザイン済み）** | ✅ |
| **Claude APIによるAI個別ヒント（6セクション）** | ✅ |
| **GASによるリード情報自動記録** | ✅ 動作確認済み |
| おすすめnote記事（ゲート後・ステージ別） | ✅（記事URL未設定） |
| 「結果をPDFで保存する」ボタン（ゲート後に表示） | ✅ |
| 印刷時にCTA・ボタン類を非表示（@media print） | ✅ |
| 無料相談CTAボタン（href="#" 仮） | ✅ |
| 免責文言（スタート・結果の両画面） | ✅ |
| LP-Cと統一のデザイントークン（ベージュ系） | ✅ |

`sindan_condition/worker/` — Cloudflare Worker（**デプロイ済み**）

| 機能 | 状態 |
|------|------|
| Claude API呼び出し（claude-haiku-4-5-20251001） | ✅ 本番稼働中 |
| 6セクション出力（分析/問い/アクション×3/メッセージ） | ✅ |
| 静的フォールバック（API未設定・エラー時） | ✅ |
| メール配信サービス連携（MailerLite等） | 🔲 コメントアウト済み・要実装 |

---

## 技術スタック

- **フロントエンド**：単一HTMLファイル（React 18 CDN + Babel Standalone）
- **ホスティング**：Cloudflare Pages（GitHubと連携・pushで自動デプロイ）
- **バックエンド**：Cloudflare Worker（`worker/src/index.js`）
- **Worker URL**：`https://sindan-condition-worker.kota-m.workers.dev`
- **AI提案生成**：Claude API（claude-haiku-4-5-20251001）・max_tokens 1500
- **フォールバック**：軸別静的テキスト6セクション（`SUGGESTIONS` 定数）
- **リード記録**：Google Apps Script → Googleスプレッドシート（GAS_URL設定済み）
- **スタイル**：インラインスタイル（ビルド不要）
- **チャート**：SVG自前実装（外部ライブラリなし）
- **PDF保存**：`window.print()` + `@media print` CSS

---

## セッション履歴

### 第4回（2026-03-21）追加作業

- **GitHubリポジトリ作成・push**：`https://github.com/kotam-san/sindan_condition.git`
- **Cloudflare Pages本番デプロイ**：`https://sindan-condition.pages.dev/`（GitHub連携・自動デプロイ）
- **Cloudflare Workerデプロイ**：`wrangler secret put ANTHROPIC_API_KEY` → `wrangler deploy`
- **GASリード記録を実装**：メアド入力時にスコア・ステージ等をGoogleスプレッドシートに自動記録
- **ゲートUIリデザイン**：「AIパーソナル・ヒント」タイトル・プレビューカード・ボタン文言変更
- **AI出力を6セクション構成に強化**：客観的分析 / コーチからの問い / アクション×3 / 専門家メッセージ

### GAS連携でのトラブルシューティング記録

GASへのデータ送信は3段階の試行錯誤があった。次回同様の実装をする際の参考に残す。

| 試行 | 方法 | 結果 | 原因 |
|------|------|------|------|
| 1回目 | POST + `application/json` | `e.postData` が undefined | GASのリダイレクトでボディが消える |
| 2回目 | GET + クエリパラメータ | `e.parameter` が undefined | リダイレクトでクエリパラメータも消える |
| 3回目 | POST + `application/x-www-form-urlencoded` + `mode: "no-cors"` | **成功** | HTMLフォームと同じ形式で CORS プリフライト不要 |

**結論：GAS Web AppへのfetchはPOST + form-encoded + no-corsが確実。**

```js
fetch(GAS_URL, {
  method: "POST",
  mode: "no-cors",
  headers: { "Content-Type": "application/x-www-form-urlencoded" },
  body: new URLSearchParams({ ... }).toString(),
})
```

GAS側では `doPost(e)` で `e.parameter.xxx` から読む。

### 第3回（2026-03-21）追加作業

- **Gated Content設計を導入**：ゲート前（無料表示）→ メアド入力 → ゲート後（AI提案）
- **Cloudflare Worker新規作成**（`worker/src/index.js` / `worker/wrangler.toml`）
- **Claude APIによるAI個別提案**：スコア・ステージ・自由記述をもとに3つの提案を生成
- **GitHub MCP設定**：`~/.claude/settings.json` に `@modelcontextprotocol/server-github` を追加
- CLAUDE.mdの整合性修正（`EMAIL_API_URL` → `WORKER_URL` に統一）

### 第2回（2026-03-21）追加作業

- 「コンパクト前」コマンドの設定（メモリ登録 + 開発ノート運用開始）
- CLAUDE.mdおよび開発ノートを次セッションへの引き継ぎ書として整備

---

## 仕様変更の記録

### AI出力を3提案 → 6セクション構成に強化（第4回）

当初：`|||` 区切りの3文（短い提案）
↓
現在：6セクション（客観的分析 / コーチからの問い / アクション×3 / 専門家メッセージ）

**理由**：初期の3提案では内容が薄く、ユーザーへの訴求力が不足していた。プロのコーチレターに近い深さの出力に変更。

### PDF直接ダウンロード → Gated Content + AI提案に再設計（第3回）

当初：PDFのみで提案を静的表示（メール不要）
↓
現在：メアド入力ゲートを設け、ゲート後にClaudeが生成した個別提案を表示。PDFもゲート後に提供。

### レーダーチャートのラベル切れ修正（第1回）

- 元の設定 `cx=110, viewBox="0 0 220 220"` では左ラベル「エネルギー」がSVG外に切れていた
- `cx=180, cy=130, viewBox="0 0 360 260"` に修正

---

## ファイル構成

```
sindan_condition/
├── CLAUDE.md              — 設計仕様・引き継ぎ書（詳細はこちらが正）
├── index.html             — 診断ツール本体
├── worker/
│   ├── wrangler.toml      — Cloudflare Worker設定
│   └── src/index.js       — Worker本体（Claude API呼び出し）
├── 開発ノート.md           ← このファイル
├── LP-C_解放を求める後継者_v3.html
├── 診断ツール_質問項目_v2.md
└── 診断ツール_実装方針_v1.md
```

---

## 次にやること

1. **動作確認**：本番URL（https://sindan-condition.pages.dev/）でフルフローをテスト
2. **CTAリンクを差し替え**（`href="#"` → 無料相談ページのURL）
3. **note記事の執筆・公開** → `NOTE_ARTICLES` のURLを設定
4. **プライバシーポリシーへの追記**（CLAUDE.mdに文案あり）
5. **平均スコアの実データ差し替え**（100件回答後に `BASELINE` 定数を更新）

---

## 保留・未決事項

| 項目 | 状況 |
|------|------|
| ~~ステップメール~~ | 廃止（AI提案 + PDF保存で代替） |
| メール配信サービス連携 | Worker内にコメントアウト済み・MailerLite等のAPI設定が必要 |
| 平均スコアの実データ差し替え | 100件回答後（現在は推定値） |
| LP-Cの本番URL | 未設定（`href="#"` のまま） |
| note記事URL | 未設定（`NOTE_ARTICLES` の url が `#` のまま） |
| プライバシーポリシーへの追記 | CLAUDE.mdに文案あり、未反映 |

---

## APIコスト感覚（参考）

Claude Haiku（claude-haiku-4-5-20251001）の料金：
- Input: $0.80 / 100万トークン
- Output: $4.00 / 100万トークン

6セクション出力の場合、1回あたり約0.4〜0.5円。月100人診断で約40〜50円。

Anthropic APIキーはCloudflare Workerのシークレットとして保存されており、ブラウザには一切露出しない。
